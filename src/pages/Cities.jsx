import React from "react";
import Table from "../components/table/Table";

const Cities = () => {
  return (
    <div>
      <h1 className="categories">Cities</h1>
      <Table />
    </div>
  );
};

export default Cities;
